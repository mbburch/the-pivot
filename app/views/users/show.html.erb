<body class="full">
  <div class="container">
    <h1>Hello, <%= User.find(session[:user_id]).username.upcase %>!</h1>
    <% if current_user.bids != nil %>
      <%= render partial: "dashboard" %>
    <% end %>
    <div class="btn btn-default btn-md"><%= link_to "Edit My Profile", edit_path %></div>
  </div>
</body>