<nav class="col-md-12 navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">BidDay</a>
    <ul class="nav navbar-nav">
      <li><a href="/items">Items</a></li>
      <li><a href="/stores">Browse Stores</a></li>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"
        aria-haspopup="true" aria-expanded="false">Categories<span class="caret">
        </span>
        </a>
        <ul class="dropdown-menu">
          <% category_list.each do |category| %>
          <li><a href="<%= category_path(category) %>"><%= category.name.titleize %></a></li>
          <% end %>
        </ul>
      </li>
    </ul>
    <ul class="nav navbar-nav navbar-right">
      <% if current_user %>
        <li><%= link_to "Hello, #{current_user.username}", dashboard_path %></li>
        <li><%= link_to "Log Out", logout_path, method: :delete %></li>
      <% else %>
        <li><a href="/register">Create Account</a></li>
        <li><a href="/login">Log In</a></li>
      <% end %>
      <li><a href="/cart">Items in Cart: <%= session[:cart].values.sum if session[:cart] %></a></li>
      <li><a id="cart" href="/cart"><span class="glyphicon glyphicon-shopping-cart"></span></a></li>
    </ul>
  </div>
</nav>

<script>
    $(document).ready(function () {
        $('.dropdown-toggle').dropdown();
    });
</script>
